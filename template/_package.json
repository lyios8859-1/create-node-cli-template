{
  "name": "{{name}}",
  "description":"{{description}}",
  "version": "{{version}}",
  "bin": {
    "{{name}}": "index.js",
    "{{command}}": "index.js"
  },
  "scripts": {
    "format": "prettier --write \"./**/*.{js,json}\""
  },
  "author": {
    "name": "{{authorName}}",
    "email": "{{authorEmail}}",
    "url": "{{authorUrl}}"
  },
  "license": "{{license}}"
}